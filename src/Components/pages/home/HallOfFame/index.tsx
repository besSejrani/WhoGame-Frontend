// Styles
import { section, title } from "./index.css";

// ====================================================================================

const HallOfFame = () => {
  return (
    <section className={section}>
      <h2 className={title}>Hall of Fame</h2>
    </section>
  );
};

export default HallOfFame;
